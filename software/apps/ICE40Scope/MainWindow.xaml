<Window x:Class="ICE40Scope.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ICE40Scope"
        mc:Ignorable="d"
        xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBox Margin="10" Grid.Row="0" Grid.Column="0" Text="{Binding ComPort}"/>
        <Button Margin="10" Grid.Row="0" Grid.Column="1" Click="btnStart_Click">Start</Button>
        <Button Margin="10" Grid.Row="0" Grid.Column="2" Click="btnStop_Click">Stop</Button>
        <oxy:PlotView Background="Transparent" Margin="10"
                      Grid.Row="1" Grid.ColumnSpan="3"
                      BorderThickness="1" BorderBrush="Black"
                      Model="{Binding ScopeData}"/>
    </Grid>
</Window>
